// Create a function that returns true if the given circles are intersecting, otherwise return false. The circles are given as two arrays containing the values in the following order:

// Radius of the circle.
// Position on the x-axis.
// Position on the y-axis.

// Examples:
// console.log(isCircleCollision([10,0,0], [10,10,10])) //true

// console.log(isCircleCollision([1,0,0], [1,10,10])) //false

// circArr -> [radius, x, y]
const isCircleCollision = (circArr1, circArr2) => {
  // sqrt((x2 - x1)**2 +(y2 - y1)**2)
  Math.sqrt();
  // distance between the origins is larger
  // than sum of both radii
};
